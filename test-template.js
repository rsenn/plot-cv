const value = 42;
const word = `simple`;
const simple = `this is a ${word}    ${value * 3} template`;

const pushHandler = async state => {
  console.log(`loading "${name}"...`);
  await load(name);
};
